#!/bin/bash

. "/local/projects/status/lib/lib.bash"

capacity=$(cat /sys/class/power_supply/BAT0/capacity)
battery_status=$(cat /sys/class/power_supply/BAT0/status | cut -b 1)

format_info "" ${capacity} "${capacity}% ${battery_status}" true
